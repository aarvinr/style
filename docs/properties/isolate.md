# Isolate

The `isolate` property can be used as an alias for `isolation`.

<!-- tabs:start -->

### **Style**

```css
:root {
  isolate: isolate;
}
```

### **CSS**

```css
:root {
  isolation: isolate;
}
```

<!-- tabs:end -->
