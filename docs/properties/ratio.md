# Ratio

The `ratio` property can be used as an alias for `aspect-ratio`.

<!-- tabs:start -->

### **Style**

```css
:root {
  ratio: 16 / 9;
}
```

### **CSS**

```css
:root {
  aspect-ratio: 16 / 9;
}
```

<!-- tabs:end -->
